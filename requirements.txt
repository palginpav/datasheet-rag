# Hugging Face Spaces runtime deps (app.py). The package itself is installed
# from the repo; these cover the demo's retrieval + UI path.
gradio>=6.0
sentence-transformers>=3.0
einops>=0.8
docling>=2.0
pydantic>=2.7
httpx>=0.27
huggingface-hub>=0.25
